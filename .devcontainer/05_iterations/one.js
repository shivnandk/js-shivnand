for (let index = 0; index < 10; index++) {
    const element = index;
    console.log(`Index is ${element+1} `);
}

//javascript mei 0 based indexing hoti hai and out of bound nahi hota
//unfined ata hai uski jagah pe